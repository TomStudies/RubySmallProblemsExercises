# PEDAC
# Unnecessary, text answer
# Answer: Different object. Just because it calls a method that mutates the caller
# Does not mean that taking a string, turning it into an array, then back into a string won't completely change it.
# From solution: If we were to use an array and call the reverse! method on words in an array, the array object would NOT change.